#include <stdio.h>

int main (int argc, char*args[]) {
    int *ptr;
    int x = 3;

    ptr = &x;

    printf("x = %d, ptr = %p\n", x, ptr);
}

#include <stdio.h>

int main (int argc, char*args[]) {
    int *ptr;
    int x = 3;

    ptr = &x;

    printf("x = %d, ptr = %p\n", x, ptr);
}